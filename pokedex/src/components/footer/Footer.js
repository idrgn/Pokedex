import { Typography } from "@mui/material";

import "./Footer.css";

export const Footer = () => {
	return (
		<footer>
			<Typography variant="body2" color="textSecondary" className="footer">
				Pokedex footer
			</Typography>
		</footer>
	);
};
